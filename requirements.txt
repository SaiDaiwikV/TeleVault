telethon==1.36.0
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
python-multipart==0.0.20
# OAuth2 social login (Google / GitHub) — plain httpx, no OAuth client lib.
httpx==0.27.2

# PostgreSQL driver — required whenever DATABASE_URL points at Postgres
# (e.g. the managed DB on Render/Railway). Unused when running on the default
# SQLite, so it's harmless to keep installed.
psycopg2-binary==2.9.10
